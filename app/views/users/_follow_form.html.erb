<% unless current_user?(@user) %>
	<div id="follow_form">
		<% if current_user.followed.include?(@user.id) %>
			<%= render 'unfollow' %>
		<% else %>
			<%= render 'follow' %>
		<% end %>
	</div>
<% end %>